/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  optimizeFonts: true,
  images: {
    unoptimized: true,
  },
 }

module.exports = {

};

module.exports = nextConfig
